import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 120,
    height: 180,
    marginRight: 10,
    borderRadius: 8,
    overflow: 'hidden',
  },
  poster: {
    width: '100%',
    height: '100%',
  },
});